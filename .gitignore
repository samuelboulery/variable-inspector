node_modules/
.DS_Store
npm-debug.log*
.vscode/

# Un seul lockfile, celui de pnpm — un package-lock.json signalerait un
# `npm install` qui a contourné le garde pnpm de ce dépôt.
package-lock.json

# Knowledge graph (rebuilt locally, never versioned)
graphify-out/

# Per-developer Claude overrides
.claude/local/
.claude/settings.local.json

# Build artifact noise (keep dist/code.js + dist/manifest.json tracked)
dist/.DS_Store

# Local workaround for stale Figma plugin imports that point at public/manifest.json
# instead of dist/manifest.json. The real entry is dist/manifest.json.
public/code.js

# Coverage reports
coverage/

# Superpowers brainstorming visual companion artifacts
.superpowers/
